# 베이스 타브 추출기 — 기술 파이프라인 설계서 v2
### (음악 영상 링크 입력 지원)

---

## 0. 시스템 개요

```
[유튜브/음악 링크 또는 오디오 파일 업로드]
        │
        ▼
Stage 0: 입력 수집 & 오디오 추출 (yt-dlp + ffmpeg)          ── 신규
        │
        ▼
Stage 1: 소스 분리 (Demucs)
        │
        ▼
Stage 2: 피치 추적 (CREPE)
        │
        ▼
Stage 3: 비트/템포 추적 (madmom, 원본 믹스 대상)
        │
        ▼
Stage 4: 노트 분절 & 양자화
        │
        ▼
Stage 5: 현/프렛 배치 (동적 계획법)
        │
        ▼
Stage 6: 타브 렌더링 (AlphaTab)
        │
        ▼
[타브 악보 출력 / GP·MusicXML 내보내기]
```

**핵심 설계 변경점**: 링크 입력은 다운로드 + Demucs 처리 시간이 합쳐져 수십 초~수 분이 걸리므로, 기존 동기 처리 구조로는 HTTP 요청이 타임아웃됩니다. **Stage 0 도입과 동시에 비동기 잡 큐 아키텍처가 필수**가 됩니다 (0.4절 참고).

---

## Stage 0: 입력 수집 & 오디오 추출 (신규)

### 0.1 URL 검증 & 메타데이터 조회

라이브러리: `yt-dlp` (`pip install yt-dlp`)

```python
import yt_dlp

def get_video_info(url: str) -> dict:
    ydl_opts = {'quiet': True, 'no_warnings': True}
    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
        info = ydl.extract_info(url, download=False)
        return {
            'title': info.get('title'),
            'duration': info.get('duration'),   # 초 단위
            'is_live': info.get('is_live', False),
        }
```

**검증 규칙**
| 조건 | 처리 |
|---|---|
| `is_live == True` | 거부 — "라이브 방송은 지원하지 않습니다" |
| `duration > 600초` | 경고 표시 후 진행 (처리 시간 비례 증가 안내) |
| 비공개/삭제/지역제한 | `yt_dlp.DownloadError` catch → 사용자에게 구체적 에러 메시지 반환 |

### 0.2 오디오 추출

```python
ydl_opts = {
    'format': 'bestaudio/best',
    'postprocessors': [{
        'key': 'FFmpegExtractAudio',
        'preferredcodec': 'wav',
        'preferredquality': '0',   # 최고 품질
    }],
    'outtmpl': f'{job_id}.%(ext)s',
}
```

- 시스템 의존성: `ffmpeg` 설치 필수
- 출력 규격: WAV / 44.1kHz / 16bit 이상 / **스테레오 유지** (Stage 1 분리 품질에 유리)

### 0.3 법적/정책 고려사항

YouTube 서비스 약관상 다운로드는 개인/연구 목적 범위가 안전합니다. 상업 서비스로 배포할 경우 별도 검토가 필요하며, 사용자가 직접 오디오 파일을 업로드하는 경로도 병행 제공하는 것을 권장합니다.

### 0.4 잡 큐 아키텍처 (필수)

```
POST /api/jobs { "url": "https://youtu.be/..." }
    → { "job_id": "abc123", "status": "queued" }

GET  /api/jobs/{job_id}
    → { "status": "processing", "stage": "separation", "progress": 40 }

GET  /api/jobs/{job_id}/result
    → { "tab_data": {...} }   # 완료 시
```

- 스택: FastAPI + Celery + Redis(브로커)
- 각 Stage 완료마다 Redis에 진행률 갱신 → 프론트는 2~3초 폴링 또는 WebSocket 구독
- 파일 저장: 임시 로컬 디스크 또는 S3, job_id 기준 디렉토리 격리, 처리 완료 후 TTL 설정해 자동 삭제

---

## Stage 1: 소스 분리 (Demucs)

```bash
demucs --two-stems=bass -n htdemucs_6s {input.wav} -o {output_dir}
```

- 모델: `htdemucs_6s` (6-stem, 베이스 분리 정확도가 기본 4-stem 모델보다 높음)
- `--two-stems=bass`로 베이스만 추출해 연산량 절감
- 출력 경로: `{output_dir}/htdemucs_6s/{track_name}/bass.wav`
- GPU 권장 (`-d cuda`), CPU 대비 5~10배 속도
- **검증 게이트**: 분리된 `bass.wav`의 RMS 에너지가 -50dB 이하면 "베이스 트랙 미검출" 에러 반환 후 파이프라인 중단

---

## Stage 2: 피치 추적 (CREPE)

```python
import crepe
import librosa

audio, sr = librosa.load('bass.wav', sr=16000, mono=True)   # CREPE는 16kHz 고정 요구
time, frequency, confidence, activation = crepe.predict(
    audio, sr,
    model_capacity='full',    # tiny/small/medium/large/full — 정확도 우선
    viterbi=True,             # 프레임간 경로 평활화, 옥타브 점프 억제
    step_size=10              # ms, 10ms = 100Hz 프레임레이트
)
```

- 신뢰도 필터: `confidence < 0.5` → 무음/노이즈로 간주, 노트 후보에서 제외
- 베이스 음역 하드 클램프(41Hz~400Hz) + 옥타브 보정:

```python
def octave_correct(freq, low=41.2, high=400.0):
    while freq < low:
        freq *= 2
    while freq > high:
        freq /= 2
    return freq
```

---

## Stage 3: 비트/템포 추적 (BeatNet)

> ⚠️ **라이선스 주의**: madmom의 `RNNBeatProcessor`/`RNNDownBeatProcessor`는 사전학습 가중치를 사용하는데, 이 모델 파일이 **CC BY-NC-SA 4.0(비상업적 전용)** 라이선스입니다. madmom 소스코드 자체는 BSD로 자유롭지만, 상업 서비스에서는 이 사전학습 모델을 쓸 수 없습니다. 대신 **BeatNet**(CC BY 4.0 — 저작자 표시만 하면 상업적 사용 가능)을 사용합니다. BeatNet은 자체 학습한 신경망으로 비트 활성화를 계산하고, madmom의 DBN 추론 알고리즘(순수 코드, BSD)만 재사용하므로 madmom의 제한된 모델 파일을 전혀 사용하지 않습니다.

```python
from BeatNet.BeatNet import BeatNet

estimator = BeatNet(1, mode='offline', inference_model='DBN', plot=[], thread=False)
output = estimator.process('original_mix.wav')
# output: numpy array [[timestamp_sec, beat_type], ...]
#   beat_type == 1 → 다운비트(마디 시작)
#   beat_type == 2 → 일반 비트

import numpy as np
beats = output[:, 0]
downbeats = output[output[:, 1] == 1, 0]
```

- **주의**: 비트 추적은 베이스 단독 트랙이 아닌 **원본 믹스**에 대해 수행 (베이스 단독은 리듬 정보 빈약)
- 출력: 비트 타임스탬프 배열(초), 마디 시작점(다운비트) 배열
- BPM = `60 / median(diff(beats))`
- 사용 시 README/크레딧에 BeatNet 저작자 표시(CC BY 4.0 요구사항) 필요

---

## Stage 4: 노트 분절 & 양자화

```python
def segment_notes(times, freqs, confidences, min_confidence=0.5, min_note_duration=0.05):
    notes = []
    current = None
    for t, f, c in zip(times, freqs, confidences):
        if c < min_confidence:
            if current:
                notes.append(current)
                current = None
            continue
        midi = librosa.hz_to_midi(f)
        if current is None:
            current = {'midi': round(midi), 'start': t, 'end': t}
        elif abs(midi - current['midi']) < 0.5:
            current['end'] = t
        else:
            notes.append(current)
            current = {'midi': round(midi), 'start': t, 'end': t}
    if current:
        notes.append(current)
    return [n for n in notes if n['end'] - n['start'] >= min_note_duration]
```

양자화 (16분음표 그리드 스냅):

```python
import numpy as np

def quantize(note_time, beats, subdivision=4):
    grid = np.concatenate([
        np.linspace(beats[i], beats[i+1], subdivision, endpoint=False)
        for i in range(len(beats) - 1)
    ])
    return grid[np.argmin(np.abs(grid - note_time))]
```

---

## Stage 5: 현/프렛 배치 (동적 계획법)

표준 4현 베이스(EADG) 개방현 기준:

| 현 | 개방현 MIDI | 개방현 주파수 |
|---|---|---|
| E (4번, 최저음) | 28 | 41.20Hz |
| A (3번) | 33 | 55.00Hz |
| D (2번) | 38 | 73.42Hz |
| G (1번, 최고음) | 43 | 98.00Hz |

```python
STRINGS = [28, 33, 38, 43]
MAX_FRET = 20

def get_candidates(midi_note):
    return [(s_idx, midi_note - open_midi)
            for s_idx, open_midi in enumerate(STRINGS)
            if 0 <= midi_note - open_midi <= MAX_FRET]

def fret_cost(prev, curr):
    if prev is None:
        return 0
    fret_dist = abs(curr[1] - prev[1])
    string_change = 1 if curr[0] != prev[0] else 0
    open_bonus = -2 if curr[1] == 0 else 0
    return fret_dist * 2 + string_change + open_bonus

def viterbi_fret_assignment(notes):
    dp = [{} for _ in notes]
    for cand in get_candidates(notes[0]['midi']):
        dp[0][cand] = (fret_cost(None, cand), None)
    for i in range(1, len(notes)):
        for cand in get_candidates(notes[i]['midi']):
            best_cost, best_prev = float('inf'), None
            for prev_cand, (prev_cost, _) in dp[i-1].items():
                cost = prev_cost + fret_cost(prev_cand, cand)
                if cost < best_cost:
                    best_cost, best_prev = cost, prev_cand
            dp[i][cand] = (best_cost, best_prev)
    # 마지막 프레임에서 최소 비용 후보 선택 후 역추적(backtrace)으로 전체 경로 복원
    last_frame = dp[-1]
    end_cand = min(last_frame, key=lambda k: last_frame[k][0])
    path = [end_cand]
    for i in range(len(notes) - 1, 0, -1):
        end_cand = dp[i][end_cand][1]
        path.insert(0, end_cand)
    return path
```

튜닝 포인트: 프렛 이동 가중치(2), 현 변경 가중치(1), 개방현 보너스(-2) — 실제 재생 테스트로 조정 권장.

---

## Stage 6: 타브 렌더링 (AlphaTab)

AlphaTex 포맷 변환 (현 번호: 1=G, 2=D, 3=A, 4=E):

```
\title "Extracted Bass Tab"
\tempo 120
.
3.2.4 5.2.4 | 0.1.4 3.2.4 |
```

프론트엔드:

```javascript
// CDN: alphaTab.min.js 로드 후
const api = new alphaTab.AlphaTabApi(element, { tex: alphatexString });
```

내보내기: AlphaTab API `exportGp()`(GuitarPro), MusicXML은 별도 컨버터 필요.

---

## 전체 기술 스택

| 레이어 | 기술 |
|---|---|
| 입력 처리 | yt-dlp, ffmpeg |
| 잡 큐 | FastAPI, Celery, Redis(또는 BSD 라이선스인 Valkey로 대체 가능) |
| 소스 분리 | Demucs (htdemucs_6s, adefossez 포크) |
| 피치 추적 | CREPE (torchcrepe) |
| 비트 추적 | BeatNet |
| 프렛 배치 | 순수 Python (DP/Viterbi) |
| 렌더링 | AlphaTab.js |
| 배포 | GPU 인스턴스 권장 |

## 라이선스 검토 결과

| 구성요소 | 라이선스 | 상업적 사용 |
|---|---|---|
| yt-dlp | Unlicense (퍼블릭 도메인) | ✅ 자유 |
| Demucs | MIT | ✅ 자유 (단, 원본 `facebookresearch/demucs`는 관리 중단·아카이브 상태 → **`adefossez/demucs` 포크** 사용) |
| CREPE / torchcrepe | MIT | ✅ 자유 |
| BeatNet | CC BY 4.0 | ✅ 저작자 표시 조건으로 자유 |
| AlphaTab | MPL-2.0 | ✅ 자유 (약한 카피레프트, 소스 공개 의무 없음) |
| madmom (소스코드만) | BSD | ✅ 자유 |
| madmom (사전학습 모델) | CC BY-NC-SA 4.0 | ❌ **비상업적 전용 — 사용 금지, BeatNet으로 대체** |
| librosa | ISC | ✅ 자유 |
| FastAPI / Celery | MIT / BSD | ✅ 자유 |
| Redis | RSALv2/SSPLv1 (v7.4+, 자체 호스팅은 무관하나 서비스형 재판매 제한) | ⚠️ 자체 앱 내부용은 문제 없음, 확실히 하려면 BSD 라이선스인 Valkey 사용 권장 |

---

## 개발 마일스톤 순서

1. **Stage 0+1**: 링크 → 베이스 wav 추출 파이프라인 (검증: 청음으로 분리 품질 확인)
2. **Stage 2**: 피치 추적 단독 테스트 (알려진 베이스 라인으로 정확도 검증)
3. **Stage 3+4**: 비트 정렬 + 노트 분절 통합
4. **Stage 5**: 프렛 배치 알고리즘 (유닛 테스트: 정답 타브와 비교)
5. **Stage 6**: AlphaTab 연동 + 프론트엔드
6. **Stage 0.4 (비동기화)**: 전체 파이프라인이 동기 처리로 검증된 후 마지막에 잡 큐로 전환

이 순서대로 가면 각 단계가 독립적으로 테스트 가능하고, 비동기 처리라는 인프라 복잡도를 가장 마지막으로 미룰 수 있습니다.
